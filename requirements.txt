# ---- Core app ----
streamlit>=1.32
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
networkx>=3.2
packaging>=23.2

# ---- Embedding backends ----
sentence-transformers>=2.3.1
transformers>=4.50.0
accelerate>=1.0.0
tokenizers>=0.15
safetensors>=0.4.5
sentencepiece>=0.2.0
bitsandbytes>=0.41.0    # Recommended for 8-bit model loading

# PyTorch (Installed via nixpacks.toml for Railway CPU optimization)
# torch>=2.2
timm>=0.9.12

# ---- Audio context paths ----
open-clip-torch>=2.24.0
ftfy                    # Required by open-clip
regex                   # Required by open-clip
tqdm                    # Required by open-clip

# Librosa & Audio
librosa>=0.10
soundfile>=0.12

# Mic recorder
streamlit-mic-recorder>=0.0.8

# ---- Optional / Advanced ----
umap-learn>=0.5.5
scipy>=1.10
seaborn>=0.13
pillow>=10.0.0
igraph>=0.11.0          # For advanced clustering in miner
leidenalg>=0.10.0       # For advanced clustering in miner

# ---- Compatibility ----
protobuf>=4.25.3
