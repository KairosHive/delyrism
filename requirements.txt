# ---- Core app ----
streamlit>=1.32
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
networkx>=3.2
packaging>=23.2

# ---- Embedding backends ----
sentence-transformers>=2.3.1
transformers>=4.50.0
accelerate>=1.0.0
tokenizers>=0.15
safetensors>=0.4.5
sentencepiece>=0.2.0    # needed for Gemma tokenization

# PyTorch (CPU wheels by default from PyPI)
torch>=2.2
timm>=0.9.12

# ---- Audio context paths ----
open-clip-torch>=2.24.0  # import: `import open_clip`

# CLAP path (via Hugging Face)
# (uses transformers + torch above)

# Librosa for loading/processing audio files and mic WAV bytes
librosa>=0.10
soundfile>=0.12

# Mic recorder Streamlit component
streamlit-mic-recorder>=0.0.8

# ---- Optional but used if available ----
umap-learn>=0.5.5      # UMAP reducer
scipy>=1.10            # ConvexHull for hulls
seaborn>=0.13          # nicer heatmaps (optional)
pillow>=10.0.0         # for Image.open

# ---- Compatibility pin to avoid known conflicts (mediapipe / google libs) ----
protobuf==4.25.3
